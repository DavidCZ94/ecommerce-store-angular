import { GroupCartProductsPipe } from './group-cart-products.pipe';

xdescribe('GroupCartProductsPipe', () => {
  it('create an instance', () => {
    const pipe = new GroupCartProductsPipe();
    expect(pipe).toBeTruthy();
  });
});
